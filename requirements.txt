# Python packages
# Install with: uv pip install -r requirements.txt
#
# IMPORTANT — system prerequisites required before installing:
#   1. NVIDIA GPU driver >= 520  (for CUDA 12.x support)
#   2. CUDA Toolkit 12.x         (required by pycuda at install time;
#                                  cupy-cuda12x bundles its own runtime)
#   See README.md for installation instructions.

numpy==2.4.2
pandas==3.0.1
matplotlib==3.10.8
ipykernel==7.2.0
jupyterlab==4.5.5
notebook-intelligence==4.3.1

# GPU / CUDA
cupy-cuda12x==14.0.1      # CuPy built against CUDA 12.x runtime
torch==2.10.0             # PyTorch (PyPI wheel includes CUDA 12 runtime)
pycuda==2026.1            # Requires nvcc in PATH during uv pip install
